---
- name: Configure VLANs (Clean & Idempotent)
  hosts: switch
  gather_facts: no
  

  tasks:

    - name: Create VLANs
      cisco.ios.ios_config:
        parents: "vlan {{ item.id }}"
        lines:
          - name {{ item.name }}
      loop: "{{ vlans }}"